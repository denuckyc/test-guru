<%= form_with model: [:admin, @question, @answer], local: true do |form| %>
  <%= render 'shared/errors', resource: @answer %>
    <p>
      <%= form.label :body %>
      <%= form.text_field :body %>
    </p>
    <p>
      <%= form.label :correct %>
      <%= form.check_box :corect %>
    </p>

    <%= form.submit %>
<% end %>
<br>
